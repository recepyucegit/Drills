<Window x:Class="GunlukUygulamasi.GunlukGoruntuleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Günlükleri Görüntüle" Height="500" Width="800">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Günlük Dosyaları Listesi -->
        <ListBox Name="lstGunlukler" SelectionChanged="lstGunlukler_SelectionChanged"/>

        <!-- Günlük İçeriği -->
        <TextBox Name="txtIcerik" Grid.Column="1" Margin="10,0,0,0"
                 AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                 IsReadOnly="True" TextWrapping="Wrap"/>
    </Grid>
</Window>
